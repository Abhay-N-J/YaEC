# YaEC

## Main docs
Start the services 

`docker-compose up -d`

View frontend

`http://localhost:8080/`

## Troubleshooting docs

if (using windows)  

- simply use `Docker Desktop`

else 

- get the container id's

  `docker ps -a`
  
- then get the logs
  
  `docker logs <container_id>`
